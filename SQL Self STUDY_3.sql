/*SELECT gender, MIN(salary) AS min_salary FROM employees GROUP BY gender;*/
/*SELECT gender, MAX(salary) AS max_salary FROM employees GROUP BY gender; */
/*SELECT gender, SUM(salary) AS total_salary FROM employees GROUP BY gender; */
/*SELECT gender, AVG(salary) AS average_salary FROM employees GROUP BY gender;*/
/*SELECT columns FROM table_A INNER JOIN table_B ON join_conditions */
/*SELECT columns FROM table_A INNER JOIN table_B  ON join_conditions1 AND join_conditions2 
INNER JOIN table_C ON join_conditions3 OR join_conditions4 */
/*SELECT
    employees.emp_id,
    employees.first_name,
    employees.last_name,
    departments.dept_name,
    departments.dept_id
  FROM employees
  INNER JOIN departments
    ON employees.emp_id = departments.emp_id; */
/*SELECT columns
  FROM table_A
  LEFT JOIN table_B ON join_conditions*/
  
  
	
